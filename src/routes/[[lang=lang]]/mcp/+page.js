// Every marketing route is prerendered — see svelte.config.js. This page is a
// dedicated route for a section that also appears on the long home page; it
// renders from the same static data files and has nothing per-request.
export const prerender = true
